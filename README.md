No further dependencies required if running from Datahub.
Dependencies: pandas, numpy, matplotlib
Optional dependencies if user wish to start from LD-Pruning and check reference results: Plink, Admixture
